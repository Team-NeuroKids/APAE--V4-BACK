services:
  database:
    image: postgres:15
    ports:
      - "5444:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: docker
      POSTGRES_DB: apae
    volumes:
      - apae:/bitnami/postgresql

volumes:
  apae:
