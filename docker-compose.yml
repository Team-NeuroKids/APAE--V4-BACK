services:
  database:
    image: bitnami/postgresql:latest
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: docker
      POSTGRES_DB: apae
    volumes:
      - apae:/bitnami/postgresql

volumes:
  apae:
